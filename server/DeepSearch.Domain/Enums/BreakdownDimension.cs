namespace DeepSearch.Domain.Enums;

/// <summary>Dimensions the result set can be grouped (broken down) by.</summary>
public enum BreakdownDimension
{
    Year,
    Gender,
    City,
    AgeGroup,
    Sector
}
