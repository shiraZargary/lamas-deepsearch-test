// Production API base URL.
// IMPORTANT: replace the host with your deployed Render API URL, e.g.
//   https://lamas-api.onrender.com/api
// (keep the trailing "/api").
export const environment = {
  apiBaseUrl: 'https://lamas-api.onrender.com/api',
};
